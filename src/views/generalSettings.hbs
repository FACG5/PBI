<h1 class='title'>الإعدادات العامة</h1>
<hr>
<div class="box">
    <form id='generalSettingForm'>
        <ul>
            <li>
                <label>سعر الدوﻻر</label>
                <input type="text" placeholder="{{#if result.length}}{{result.0.dataValues.dolar}}{{/if}}" name="dolar">
            </li>
            <li>
                <label>القيمة الضريبية</label>
                <input type="text" placeholder="{{#if result.length}}{{result.0.dataValues.taxValue}}{{/if}}" name="taxValue">
            </li>
            <li>
                <label>نسبة الادخار</label>
                <input type="text" placeholder="{{#if result.length}}{{result.0.dataValues.savingRatio}}{{/if}}" name="savingRatio">
            </li>
            <li>
                <label>نسبة مساهمة المعهد في الضمان</label>
                <input type="text" placeholder="{{#if result.length}}{{result.0.dataValues.contributionInstitute}}{{/if}}" name="contributionInstitute">
            </li>
            <li>
                <label>نسبة مساهمة الموظف في الضمان</label>
                <input type="text" placeholder="{{#if result.length}}{{result.0.dataValues.contributionEmployee}}{{/if}}" name="contributionEmployee">
            </li>
            <li>
                <label>اعفاء مقيم</label>
                <input type="text" placeholder="{{#if result.length}}{{result.0.dataValues.exemptionResident}}{{/if}}" name="exemptionResident">
            </li>
        </ul>
    </form>
        <button id = 'saveSetting'>حفظ</button>
</div>
